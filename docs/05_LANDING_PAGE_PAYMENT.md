# DealSpy.eu - Landing Page & Payment System
## Cursor Prompt - Teljes Specifikáció

---

# RÉSZ 1: LANDING PAGE SZÖVEGEK ÉS STRUKTÚRA

## 1.1 HERO SECTION

### Magyar verzió:

**Főcím:**
```
Csődvagyon és aukciók egész Európából.
Automatikusan, magyarul, az öledbe.
```

**Alcím:**
```
A DealSpy figyeli helyetted a magyar, osztrák és német csődvagyon portálokat. 
Értesítünk, ha megjelenik amit keresel – legyen az szerver, gép, jármű vagy ingatlan.
```

**CTA gombok:**
```
[🚀 Próbáld ki ingyen 14 napig]     [▶ Nézd meg, hogyan működik]
```

**Trust badges (sor):**
```
✓ 10+ platform figyelése    ✓ Napi automatikus frissítés    ✓ Automatikus fordítás    ✓ 30-70% a piaci ár alatt
```

### Angol verzió:

**Headline:**
```
EU Insolvency Assets & Auctions.
Delivered to you automatically.
```

**Subheadline:**
```
DealSpy monitors bankruptcy and auction platforms across Hungary, Austria, and Germany.
Get notified when relevant deals appear – servers, machinery, vehicles, or real estate.
```

### Német verzió:

**Headline:**
```
Insolvenz-Vermögen & Auktionen aus ganz Europa.
Automatisch. Übersetzt. Direkt zu Ihnen.
```

**Subheadline:**
```
DealSpy überwacht Insolvenz- und Auktionsportale in Ungarn, Österreich und Deutschland.
Erhalten Sie Benachrichtigungen über relevante Angebote – Server, Maschinen, Fahrzeuge oder Immobilien.
```

---

## 1.2 PROBLEM SECTION

**Fejléc (HU):**
```
Ismered ezt a problémát?
```

**3 Pain Point kártya:**

```
┌─────────────────────────┐  ┌─────────────────────────┐  ┌─────────────────────────┐
│ 😫                      │  │ 🇩🇪                      │  │ ⏰                      │
│ TÚL SOK PLATFORM        │  │ NYELVI AKADÁLY          │  │ LEKÉSEL A DEALEKRŐL    │
│                         │  │                         │  │                         │
│ 10+ különböző oldalt    │  │ A legjobb lehetőségek   │  │ Mire észreveszed,      │
│ kellene naponta nézni.  │  │ németül vannak. Ki ér   │  │ már lejárt az aukció   │
│ Ki ér rá erre?          │  │ rá ezt fordítgatni?     │  │ vagy más vitte el.     │
└─────────────────────────┘  └─────────────────────────┘  └─────────────────────────┘
```

**Megoldás (HU):**
```
💡 A DealSpy megoldja mindezt.

Egyetlen felületen figyeljük az összes releváns platformot, 
automatikusan lefordítjuk a találatokat, és azonnal értesítünk.
Te csak a vásárlásra koncentrálj.
```

---

## 1.3 HOW IT WORKS SECTION

**Fejléc (HU):**
```
Hogyan működik?
```

**3 lépés:**

```
    ①                          ②                          ③
┌─────────┐              ┌─────────┐              ┌─────────┐
│  ⚙️     │      →       │  🔍     │      →       │  🔔     │
│ BEÁLLÍT │              │ FIGYEL  │              │ ÉRTESÍT │
└─────────┘              └─────────┘              └─────────┘

Válaszd ki a           A DealSpy naponta        Azonnal értesítünk
kategóriákat,          naponta végigfésüli       push üzenetben,
országokat és          a platformokat és        emailben vagy
kulcsszavakat.         megtalálja a releváns    Telegramon, ha
                       lehetőségeket.           új deal érkezik.

⏱️ 2 perc beállítás    🤖 Teljesen automatikus   📱 Valós idejű alertek
```

---

## 1.4 SOURCES SECTION

**Fejléc (HU):**
```
Ezeket a platformokat figyeljük
```

**Platform grid:**

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│  🇭🇺 MAGYARORSZÁG                                                            │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ [EÉR logo]                                                             │ │
│  │ Elektronikus Értékesítési Rendszer                                     │ │
│  │ A hivatalos magyar csődvagyon értékesítési platform                    │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  🇦🇹 AUSZTRIA                                                               │
│  ┌──────────────────────────────┐ ┌──────────────────────────────┐         │
│  │ [Ediktsdatei logo]           │ │ [KSV1870 logo]               │         │
│  │ Ediktsdatei                  │ │ KSV1870                      │         │
│  │ Hivatalos bírósági           │ │ Osztrák hitelezővédelmi      │         │
│  │ hirdetmények                 │ │ csődlista                    │         │
│  └──────────────────────────────┘ └──────────────────────────────┘         │
│                                                                              │
│  🇩🇪 NÉMETORSZÁG                                                            │
│  ┌────────────────┐ ┌────────────────┐ ┌────────────────┐ ┌────────────────┐│
│  │ Insolvenz-     │ │ NetBid         │ │ Proventura     │ │ Hämmerle       ││
│  │ bekanntm.      │ │                │ │                │ │                ││
│  │ Hivatalos DE   │ │ Ipari aukciók  │ │ Gépek, IT,     │ │ Online és      ││
│  │ csődhirdetések │ │ egész EU-ból   │ │ járművek       │ │ live aukciók   ││
│  └────────────────┘ └────────────────┘ └────────────────┘ └────────────────┘│
│                                                                              │
│  🇪🇺 EU-SZINTŰ PIACTEREK                                                    │
│  ┌────────────────┐ ┌────────────────┐ ┌────────────────┐ ┌────────────────┐│
│  │ Machineseeker  │ │ Mascus         │ │ TruckScout24   │ │ AssetOrb       ││
│  │                │ │                │ │                │ │                ││
│  │ Európa #1      │ │ Építőipari és  │ │ Teherautók,    │ │ Ipari aukciók  ││
│  │ géppiac        │ │ mezőgazdasági  │ │ haszongépjárm. │ │ és csődvagyon  ││
│  └────────────────┘ └────────────────┘ └────────────────┘ └────────────────┘│
│                                                                              │
│  📈 Folyamatosan bővülő forráslista                                         │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 1.5 FEATURES SECTION

**Fejléc (HU):**
```
Minden, amire szükséged van
```

**Feature grid (2x3):**

```
┌─────────────────────────────────┐  ┌─────────────────────────────────┐
│ 🌍 TÖBBNYELVŰ FORDÍTÁS          │  │ 🎯 INTELLIGENS SZŰRÉS           │
│                                 │  │                                 │
│ A német hirdetések automatiku-  │  │ Kategória, ország, kulcsszó    │
│ san magyarul és angolul is      │  │ alapján csak azt kapod, ami    │
│ megjelennek. Nem kell           │  │ tényleg érdekel. Semmi spam,   │
│ fordítgatni.                    │  │ csak releváns találatok.       │
└─────────────────────────────────┘  └─────────────────────────────────┘

┌─────────────────────────────────┐  ┌─────────────────────────────────┐
│ 🔔 AZONNALI ÉRTESÍTÉSEK         │  │ 📊 NAPI ÖSSZEFOGLALÓ            │
│                                 │  │                                 │
│ Push notification, Telegram     │  │ Ha nem szereted az azonnali    │
│ vagy email – te döntöd el,      │  │ értesítéseket, napi email      │
│ hogyan értesítsünk. Akár        │  │ digest-ben is megkaphatod      │
│ azonnal, akár összefoglalóban.  │  │ az összes új lehetőséget.      │
└─────────────────────────────────┘  └─────────────────────────────────┘

┌─────────────────────────────────┐  ┌─────────────────────────────────┐
│ ⏰ HATÁRIDŐ FIGYELMEZTETÉS      │  │ 💰 30-70% MEGTAKARÍTÁS          │
│                                 │  │                                 │
│ Látod, mikor jár le az aukció   │  │ A csődvagyonból és aukciókból  │
│ vagy pályázat. Soha többé nem   │  │ beszerzett eszközök jellemzően │
│ késel le egy jó lehetőséget.    │  │ töredék áron kelnek el.        │
└─────────────────────────────────┘  └─────────────────────────────────┘
```

---

## 1.6 TARGET AUDIENCE SECTION

**Fejléc (HU):**
```
Kiknek ajánljuk?
```

**Audience cards:**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│  👨‍💼 VÁLLALKOZÓK, KKV-K               👷 HASZNÁLTGÉP KERESKEDŐK             │
│  Olcsóbb IT infrastruktúra,           Beszerzési csatorna bővítése,        │
│  irodabútor, gépek beszerzése         versenyelőny a piacon                │
│  a cég számára                                                              │
│                                                                             │
│  💼 BEFEKTETŐK                         🏛️ M&A TANÁCSADÓK                    │
│  Értéken aluli eszközök,              Deal flow monitoring,                │
│  ingatlanok, cégrészek                ügyfelek számára lehetőségek        │
│  felkutatása                          azonosítása                          │
│                                                                             │
│  🔧 SZERVIZEK, MŰHELYEK               📦 RAKTÁROZÁS, LOGISZTIKA            │
│  Kedvezményes szerszámok,             Targoncák, raktári eszközök,        │
│  diagnosztikai eszközök,              polcrendszerek beszerzése           │
│  berendezések                         töredék áron                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 1.7 PRICING SECTION

**Fejléc (HU):**
```
Egyszerű, átlátható árazás
```

**Alcím:**
```
Válaszd ki a számodra megfelelő csomagot. 14 napos ingyenes próbaidőszak minden csomaghoz.
```

**Pricing cards:**

```
┌───────────────────────┐  ┌───────────────────────┐  ┌───────────────────────┐
│                       │  │ ⭐ NÉPSZERŰ           │  │                       │
│       STARTER         │  │       PRO             │  │     ENTERPRISE        │
│                       │  │                       │  │                       │
│      €19/hó           │  │      €49/hó           │  │      €149/hó          │
│   (éves: €15/hó)      │  │   (éves: €39/hó)      │  │   (éves: €119/hó)     │
│                       │  │                       │  │                       │
├───────────────────────┤  ├───────────────────────┤  ├───────────────────────┤
│                       │  │                       │  │                       │
│ ✓ 1 ország            │  │ ✓ 3 ország            │  │ ✓ Összes ország       │
│   (HU vagy AT vagy DE)│  │   (HU + AT + DE)      │  │                       │
│                       │  │                       │  │ ✓ Összes platform     │
│ ✓ 3 platform          │  │ ✓ Összes platform     │  │                       │
│                       │  │                       │  │ ✓ Összes kategória    │
│ ✓ 2 kategória         │  │ ✓ Összes kategória    │  │                       │
│                       │  │                       │  │ ✓ Korlátlan kulcsszó  │
│ ✓ 5 kulcsszó          │  │ ✓ 20 kulcsszó         │  │                       │
│                       │  │                       │  │ ✓ Azonnali értesítés  │
│ ✓ Napi email digest   │  │ ✓ Azonnali értesítés  │  │   (Push + Telegram +  │
│                       │  │   (Push + Email)      │  │    Email + Slack)     │
│ ✗ Azonnali push       │  │                       │  │                       │
│                       │  │ ✓ Telegram bot        │  │ ✓ API hozzáférés      │
│ ✗ Telegram            │  │                       │  │                       │
│                       │  │ ✓ Prioritás support   │  │ ✓ Dedikált support    │
│ ✓ Email support       │  │                       │  │                       │
│                       │  │                       │  │ ✓ Egyedi integráció   │
│                       │  │                       │  │                       │
├───────────────────────┤  ├───────────────────────┤  ├───────────────────────┤
│                       │  │                       │  │                       │
│ [Próbáld ki ingyen]   │  │ [Próbáld ki ingyen]   │  │ [Kapcsolatfelvétel]   │
│                       │  │                       │  │                       │
└───────────────────────┘  └───────────────────────┘  └───────────────────────┘

                    💳 Biztonságos fizetés Stripe-on keresztül
                    🔄 Bármikor lemondható | 💰 14 napos pénzvisszafizetés
```

---

## 1.8 FAQ SECTION

**Fejléc (HU):**
```
Gyakran ismételt kérdések
```

**FAQ items:**

```
▼ Hogyan működik a 14 napos próbaidőszak?
  
  A regisztráció után 14 napig ingyenesen használhatod a választott csomag 
  összes funkcióját. Bankkártya adatokat csak a próbaidőszak végén kérünk, 
  ha folytatni szeretnéd. Nincs rejtett költség, bármikor megszakíthatod.

▼ Milyen fizetési módokat fogadtok el?
  
  Bankkártyás fizetést (Visa, Mastercard, American Express) a Stripe 
  biztonságos fizetési platformján keresztül. Éves előfizetésnél banki 
  átutalás is lehetséges.

▼ Lemondhatom bármikor az előfizetést?
  
  Igen, az előfizetés bármikor lemondható. A lemondás után a már kifizetett 
  időszak végéig használhatod a szolgáltatást. Éves előfizetésnél az első 
  14 napban teljes visszatérítést biztosítunk.

▼ Honnan származnak az adatok?
  
  Kizárólag nyilvános, hivatalos forrásokból: bírósági hirdetmények, 
  felszámolói portálok, aukciós házak weboldalai. Minden adat publikusan 
  elérhető – mi csak összegyűjtjük és szűrjük neked.

▼ Milyen gyakran frissülnek az adatok?
  
  Naponta egyszer scrape (09:30 CET), értesítés 10:00 CET, digest 15:00 CET (Vercel Hobby). 
  Ha új releváns deal érkezik, értesítünk (Pro és Enterprise csomag).

▼ Működik mobilon is?
  
  Igen! A push értesítések és a Telegram bot mobilon is tökéletesen működik. 
  Az email digest szintén mobile-responsive formátumban érkezik.

▼ Mi történik, ha egy platform megváltozik?
  
  Folyamatosan karbantartjuk a scrapereket. Ha egy forrás átalakul, 
  48 órán belül frissítjük a rendszert. Enterprise ügyfeleket 
  egyedileg értesítjük az esetleges kiesésekről.

▼ Van API hozzáférés?
  
  Az Enterprise csomagban igen. REST API-n keresztül saját rendszerbe 
  integrálhatod a deal feed-et. Dokumentáció és support jár hozzá.
```

---

## 1.9 FINAL CTA SECTION

**Fejléc (HU):**
```
Ne maradj le a következő jó üzletről
```

**Szöveg:**
```
Csatlakozz azokhoz, akik már automatikusan kapják a legjobb csődvagyon 
és aukciós lehetőségeket. A beállítás 2 percet vesz igénybe.
```

**CTA:**
```
[🚀 Ingyenes próba indítása – bankkártya nélkül]
```

**Trust:**
```
🔒 256-bit SSL titkosítás | 🇪🇺 GDPR kompatibilis | ⭐ 4.8/5 értékelés
```

---

## 1.10 FOOTER

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│  🔍 DealSpy.eu                                                              │
│  EU csődvagyon monitoring                                                   │
│                                                                             │
│  TERMÉK              FORRÁSOK           CÉGINFO           KAPCSOLAT         │
│  Funkciók            EÉR (HU)           Rólunk            info@dealspy.eu   │
│  Árazás              Ediktsdatei (AT)   Impressum         +36 XX XXX XXXX   │
│  FAQ                 NetBid (DE)        GDPR/Privacy                        │
│  API Docs            Proventura (DE)    ÁSZF                                │
│                      Machineseeker      Cookie Policy                       │
│                                                                             │
│  ─────────────────────────────────────────────────────────────────────────  │
│                                                                             │
│  © 2026 DealSpy.eu | Progile Tanácsadó Kft. | Minden jog fenntartva         │
│                                                                             │
│  [LinkedIn] [Twitter/X] [Facebook]                                          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

# RÉSZ 2: ÁRAZÁSI LOGIKA

## 2.1 Pricing Tiers

```typescript
type PricingTier = 'starter' | 'pro' | 'enterprise';
type BillingCycle = 'monthly' | 'yearly';

const PRICING = {
  starter: {
    monthly: 19,  // €/hó
    yearly: 15,   // €/hó (éves fizetésnél)
    limits: {
      countries: 1,
      platforms: 3,
      categories: 2,
      keywords: 5,
      channels: ['email'],
    },
  },
  pro: {
    monthly: 49,
    yearly: 39,
    limits: {
      countries: 3,
      platforms: -1, // unlimited
      categories: -1,
      keywords: 20,
      channels: ['email', 'push', 'telegram'],
    },
  },
  enterprise: {
    monthly: 149,
    yearly: 119,
    limits: {
      countries: -1,
      platforms: -1,
      categories: -1,
      keywords: -1,
      channels: ['email', 'push', 'telegram', 'slack', 'api'],
    },
  },
};

// Éves fizetésnél a megtakarítás
// Starter: €19*12 = €228 → €15*12 = €180 (€48 megtakarítás, ~21%)
// Pro: €49*12 = €588 → €39*12 = €468 (€120 megtakarítás, ~20%)
// Enterprise: €149*12 = €1788 → €119*12 = €1428 (€360 megtakarítás, ~20%)
```

## 2.2 Stripe Termékek

```
Stripe Products:
├── dealspy_starter_monthly    (€19/hó)
├── dealspy_starter_yearly     (€180/év = €15/hó)
├── dealspy_pro_monthly        (€49/hó)
├── dealspy_pro_yearly         (€468/év = €39/hó)
├── dealspy_enterprise_monthly (€149/hó)
└── dealspy_enterprise_yearly  (€1428/év = €119/hó)
```

---

# RÉSZ 3: USER FLOW

## 3.1 Registration & Subscription Flow

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           REGISTRATION FLOW                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. LANDING PAGE                                                            │
│     │                                                                       │
│     └──► User clicks "Próbáld ki ingyen" on any pricing card               │
│                                                                             │
│  2. STEP 1: ACCOUNT                                                         │
│     │                                                                       │
│     │   ┌─────────────────────────────────────────────────────────┐        │
│     │   │ Fiók létrehozása                         Step 1/4 ●○○○ │        │
│     │   │                                                         │        │
│     │   │ Email *                                                 │        │
│     │   │ ┌─────────────────────────────────────────────────────┐ │        │
│     │   │ │ pelda@email.com                                     │ │        │
│     │   │ └─────────────────────────────────────────────────────┘ │        │
│     │   │                                                         │        │
│     │   │ Jelszó *                                                │        │
│     │   │ ┌─────────────────────────────────────────────────────┐ │        │
│     │   │ │ ••••••••                                            │ │        │
│     │   │ └─────────────────────────────────────────────────────┘ │        │
│     │   │                                                         │        │
│     │   │ Nyelv                                                   │        │
│     │   │ [🇭🇺 Magyar ▾]                                          │        │
│     │   │                                                         │        │
│     │   │                              [Tovább →]                 │        │
│     │   └─────────────────────────────────────────────────────────┘        │
│     │                                                                       │
│     └──► Continues to Step 2                                               │
│                                                                             │
│  3. STEP 2: PREFERENCES                                                     │
│     │                                                                       │
│     │   ┌─────────────────────────────────────────────────────────┐        │
│     │   │ Mit keresel?                             Step 2/4 ●●○○ │        │
│     │   │                                                         │        │
│     │   │ Kategóriák *                                            │        │
│     │   │ ☑ IT / Szerverek    ☑ Ipari gépek                      │        │
│     │   │ ☐ Járművek          ☐ Ingatlan                         │        │
│     │   │                                                         │        │
│     │   │ Országok *                                              │        │
│     │   │ ☑ 🇭🇺 Magyarország   ☑ 🇦🇹 Ausztria   ☑ 🇩🇪 Németország │        │
│     │   │                                                         │        │
│     │   │ Kulcsszavak (opcionális)                                │        │
│     │   │ ┌─────────────────────────────────────────────────────┐ │        │
│     │   │ │ szerver, GPU, rack, CNC                             │ │        │
│     │   │ └─────────────────────────────────────────────────────┘ │        │
│     │   │ ℹ️ Vesszővel elválasztva                                │        │
│     │   │                                                         │        │
│     │   │                      [← Vissza]  [Tovább →]             │        │
│     │   └─────────────────────────────────────────────────────────┘        │
│     │                                                                       │
│     └──► Continues to Step 3                                               │
│                                                                             │
│  4. STEP 3: NOTIFICATIONS                                                   │
│     │                                                                       │
│     │   ┌─────────────────────────────────────────────────────────┐        │
│     │   │ Hogyan értesítsünk?                      Step 3/4 ●●●○ │        │
│     │   │                                                         │        │
│     │   │ ☑ 📧 Email összefoglaló                                │        │
│     │   │   └─ Naponta 15:00-kor a napi deal-ek                  │        │
│     │   │                                                         │        │
│     │   │ ☑ 🔔 Böngésző push értesítés                           │        │
│     │   │   └─ Azonnal, ha új releváns deal érkezik              │        │
│     │   │                                                         │        │
│     │   │ ☐ 📱 Telegram bot                                      │        │
│     │   │   └─ @DealSpyBot – azonnali értesítés                  │        │
│     │   │                                                         │        │
│     │   │                      [← Vissza]  [Tovább →]             │        │
│     │   └─────────────────────────────────────────────────────────┘        │
│     │                                                                       │
│     └──► If Push selected: browser permission prompt                       │
│     └──► If Telegram selected: show bot link                               │
│                                                                             │
│  5. STEP 4: PLAN SELECTION & PAYMENT                                        │
│     │                                                                       │
│     │   ┌─────────────────────────────────────────────────────────┐        │
│     │   │ Válassz csomagot                         Step 4/4 ●●●● │        │
│     │   │                                                         │        │
│     │   │ A beállításaid alapján ajánlott: PRO csomag            │        │
│     │   │ (3 ország, 2 kategória kiválasztva)                    │        │
│     │   │                                                         │        │
│     │   │ ┌─────────┐ ┌─────────┐ ┌─────────┐                    │        │
│     │   │ │ STARTER │ │⭐ PRO  │ │ENTERPRI.│                    │        │
│     │   │ │  €19/hó │ │  €49/hó │ │ €149/hó │                    │        │
│     │   │ │[Választ]│ │[Választ]│ │[Választ]│                    │        │
│     │   │ └─────────┘ └─────────┘ └─────────┘                    │        │
│     │   │                                                         │        │
│     │   │ Fizetési gyakoriság                                     │        │
│     │   │ ○ Havi fizetés (€49/hó)                                │        │
│     │   │ ● Éves fizetés (€39/hó) – 20% megtakarítás 🏷️          │        │
│     │   │                                                         │        │
│     │   │ ─────────────────────────────────────────────────────── │        │
│     │   │                                                         │        │
│     │   │ Összesen: €468/év (14 nap ingyenes próba után)         │        │
│     │   │                                                         │        │
│     │   │ ☑ Elfogadom az ÁSZF-et és az Adatvédelmi tájékoztatót │        │
│     │   │                                                         │        │
│     │   │         [🔒 Biztonságos fizetés indítása]               │        │
│     │   │                                                         │        │
│     │   │ 💳 Stripe secure checkout                              │        │
│     │   │ A próbaidőszak alatt nem terhelünk semmit.             │        │
│     │   └─────────────────────────────────────────────────────────┘        │
│     │                                                                       │
│     └──► Stripe Checkout redirect                                          │
│                                                                             │
│  6. STRIPE CHECKOUT                                                         │
│     │                                                                       │
│     └──► Stripe hosted checkout page                                       │
│         (kártya adatok megadása)                                           │
│                                                                             │
│  7. SUCCESS PAGE                                                            │
│     │                                                                       │
│     │   ┌─────────────────────────────────────────────────────────┐        │
│     │   │                                                         │        │
│     │   │              ✅ Sikeres regisztráció!                   │        │
│     │   │                                                         │        │
│     │   │   A 14 napos próbaidőszakod elkezdődött.               │        │
│     │   │   Az első értesítést ma 15:00-kor kapod.               │        │
│     │   │                                                         │        │
│     │   │   Ha Telegramot választottál:                          │        │
│     │   │   [📱 Telegram bot összekapcsolása]                    │        │
│     │   │                                                         │        │
│     │   │   [→ Beállítások megtekintése]                         │        │
│     │   │                                                         │        │
│     │   └─────────────────────────────────────────────────────────┘        │
│     │                                                                       │
│     └──► User can access dashboard/settings                                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

# RÉSZ 4: DATABASE SCHEMA KIEGÉSZÍTÉS

```sql
-- Kiegészítés a meglévő users táblához

ALTER TABLE users ADD COLUMN IF NOT EXISTS password_hash TEXT;
ALTER TABLE users ADD COLUMN IF NOT EXISTS subscription_tier TEXT DEFAULT 'trial' 
  CHECK (subscription_tier IN ('trial', 'starter', 'pro', 'enterprise', 'cancelled'));
ALTER TABLE users ADD COLUMN IF NOT EXISTS subscription_status TEXT DEFAULT 'trialing'
  CHECK (subscription_status IN ('trialing', 'active', 'past_due', 'cancelled', 'unpaid'));
ALTER TABLE users ADD COLUMN IF NOT EXISTS billing_cycle TEXT DEFAULT 'monthly'
  CHECK (billing_cycle IN ('monthly', 'yearly'));
ALTER TABLE users ADD COLUMN IF NOT EXISTS trial_ends_at TIMESTAMP WITH TIME ZONE;
ALTER TABLE users ADD COLUMN IF NOT EXISTS subscription_ends_at TIMESTAMP WITH TIME ZONE;
ALTER TABLE users ADD COLUMN IF NOT EXISTS stripe_customer_id TEXT;
ALTER TABLE users ADD COLUMN IF NOT EXISTS stripe_subscription_id TEXT;

-- Payments/invoices log
CREATE TABLE IF NOT EXISTS payments (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  stripe_payment_intent_id TEXT,
  stripe_invoice_id TEXT,
  amount DECIMAL NOT NULL,
  currency TEXT DEFAULT 'EUR',
  status TEXT CHECK (status IN ('pending', 'succeeded', 'failed', 'refunded')),
  tier TEXT,
  billing_cycle TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX idx_payments_user_id ON payments(user_id);
CREATE INDEX idx_users_stripe_customer_id ON users(stripe_customer_id);
```

---

# RÉSZ 5: STRIPE INTEGRATION

## 5.1 Environment Variables

```env
# Stripe
STRIPE_SECRET_KEY=sk_live_xxx
STRIPE_PUBLISHABLE_KEY=pk_live_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Stripe Price IDs (create these in Stripe Dashboard)
STRIPE_PRICE_STARTER_MONTHLY=price_xxx
STRIPE_PRICE_STARTER_YEARLY=price_xxx
STRIPE_PRICE_PRO_MONTHLY=price_xxx
STRIPE_PRICE_PRO_YEARLY=price_xxx
STRIPE_PRICE_ENTERPRISE_MONTHLY=price_xxx
STRIPE_PRICE_ENTERPRISE_YEARLY=price_xxx
```

## 5.2 Stripe Checkout Session

```typescript
// lib/stripe.ts

import Stripe from 'stripe';

const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!, {
  apiVersion: '2023-10-16',
});

const PRICE_IDS = {
  starter: {
    monthly: process.env.STRIPE_PRICE_STARTER_MONTHLY!,
    yearly: process.env.STRIPE_PRICE_STARTER_YEARLY!,
  },
  pro: {
    monthly: process.env.STRIPE_PRICE_PRO_MONTHLY!,
    yearly: process.env.STRIPE_PRICE_PRO_YEARLY!,
  },
  enterprise: {
    monthly: process.env.STRIPE_PRICE_ENTERPRISE_MONTHLY!,
    yearly: process.env.STRIPE_PRICE_ENTERPRISE_YEARLY!,
  },
};

export async function createCheckoutSession(
  userId: string,
  email: string,
  tier: 'starter' | 'pro' | 'enterprise',
  billingCycle: 'monthly' | 'yearly'
): Promise<string> {
  const priceId = PRICE_IDS[tier][billingCycle];
  
  const session = await stripe.checkout.sessions.create({
    mode: 'subscription',
    payment_method_types: ['card'],
    customer_email: email,
    line_items: [
      {
        price: priceId,
        quantity: 1,
      },
    ],
    subscription_data: {
      trial_period_days: 14,
      metadata: {
        userId,
        tier,
        billingCycle,
      },
    },
    metadata: {
      userId,
      tier,
      billingCycle,
    },
    success_url: `${process.env.NEXT_PUBLIC_APP_URL}/success?session_id={CHECKOUT_SESSION_ID}`,
    cancel_url: `${process.env.NEXT_PUBLIC_APP_URL}/register?cancelled=true`,
    locale: 'hu', // or 'en', 'de' based on user preference
  });
  
  return session.url!;
}

export async function createBillingPortalSession(customerId: string): Promise<string> {
  const session = await stripe.billingPortal.sessions.create({
    customer: customerId,
    return_url: `${process.env.NEXT_PUBLIC_APP_URL}/settings`,
  });
  
  return session.url;
}
```

## 5.3 Stripe Webhook Handler

```typescript
// app/api/stripe/webhook/route.ts

import { NextRequest, NextResponse } from 'next/server';
import Stripe from 'stripe';
import { supabaseAdmin } from '@/lib/db/supabase';

const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!);

export async function POST(request: NextRequest) {
  const body = await request.text();
  const signature = request.headers.get('stripe-signature')!;
  
  let event: Stripe.Event;
  
  try {
    event = stripe.webhooks.constructEvent(
      body,
      signature,
      process.env.STRIPE_WEBHOOK_SECRET!
    );
  } catch (err) {
    return NextResponse.json({ error: 'Invalid signature' }, { status: 400 });
  }
  
  switch (event.type) {
    case 'checkout.session.completed': {
      const session = event.data.object as Stripe.Checkout.Session;
      const { userId, tier, billingCycle } = session.metadata!;
      
      // Update user with Stripe info
      await supabaseAdmin.from('users').update({
        stripe_customer_id: session.customer as string,
        stripe_subscription_id: session.subscription as string,
        subscription_tier: tier,
        subscription_status: 'trialing',
        billing_cycle: billingCycle,
        trial_ends_at: new Date(Date.now() + 14 * 24 * 60 * 60 * 1000).toISOString(),
      }).eq('id', userId);
      
      break;
    }
    
    case 'customer.subscription.updated': {
      const subscription = event.data.object as Stripe.Subscription;
      const customerId = subscription.customer as string;
      
      const status = subscription.status === 'active' ? 'active' :
                     subscription.status === 'trialing' ? 'trialing' :
                     subscription.status === 'past_due' ? 'past_due' :
                     subscription.status === 'canceled' ? 'cancelled' : 'unpaid';
      
      await supabaseAdmin.from('users').update({
        subscription_status: status,
        subscription_ends_at: new Date(subscription.current_period_end * 1000).toISOString(),
      }).eq('stripe_customer_id', customerId);
      
      break;
    }
    
    case 'customer.subscription.deleted': {
      const subscription = event.data.object as Stripe.Subscription;
      const customerId = subscription.customer as string;
      
      await supabaseAdmin.from('users').update({
        subscription_status: 'cancelled',
        subscription_tier: 'cancelled',
      }).eq('stripe_customer_id', customerId);
      
      break;
    }
    
    case 'invoice.payment_succeeded': {
      const invoice = event.data.object as Stripe.Invoice;
      const customerId = invoice.customer as string;
      
      // Log payment
      const { data: user } = await supabaseAdmin
        .from('users')
        .select('id, subscription_tier, billing_cycle')
        .eq('stripe_customer_id', customerId)
        .single();
      
      if (user) {
        await supabaseAdmin.from('payments').insert({
          user_id: user.id,
          stripe_invoice_id: invoice.id,
          amount: invoice.amount_paid / 100,
          currency: invoice.currency.toUpperCase(),
          status: 'succeeded',
          tier: user.subscription_tier,
          billing_cycle: user.billing_cycle,
        });
      }
      
      break;
    }
    
    case 'invoice.payment_failed': {
      const invoice = event.data.object as Stripe.Invoice;
      const customerId = invoice.customer as string;
      
      await supabaseAdmin.from('users').update({
        subscription_status: 'past_due',
      }).eq('stripe_customer_id', customerId);
      
      // TODO: Send email notification about failed payment
      
      break;
    }
  }
  
  return NextResponse.json({ received: true });
}
```

## 5.4 Subscription Limit Enforcement

```typescript
// lib/subscription.ts

import { User } from './db/supabase';

const TIER_LIMITS = {
  trial: { countries: 3, platforms: -1, categories: -1, keywords: 20 },
  starter: { countries: 1, platforms: 3, categories: 2, keywords: 5 },
  pro: { countries: 3, platforms: -1, categories: -1, keywords: 20 },
  enterprise: { countries: -1, platforms: -1, categories: -1, keywords: -1 },
  cancelled: { countries: 0, platforms: 0, categories: 0, keywords: 0 },
};

export function canUserAccessFeature(user: User, feature: string): boolean {
  const tier = user.subscription_tier || 'trial';
  const status = user.subscription_status;
  
  // Check if subscription is active
  if (status === 'cancelled' || status === 'unpaid') {
    return false;
  }
  
  // Check trial expiry
  if (status === 'trialing' && user.trial_ends_at) {
    if (new Date(user.trial_ends_at) < new Date()) {
      return false;
    }
  }
  
  // Feature-specific checks
  switch (feature) {
    case 'push':
      return tier === 'pro' || tier === 'enterprise' || tier === 'trial';
    case 'telegram':
      return tier === 'pro' || tier === 'enterprise' || tier === 'trial';
    case 'api':
      return tier === 'enterprise';
    case 'slack':
      return tier === 'enterprise';
    default:
      return true;
  }
}

export function getUserLimits(tier: string) {
  return TIER_LIMITS[tier as keyof typeof TIER_LIMITS] || TIER_LIMITS.trial;
}

export function validateUserSettings(user: User, settings: Partial<User>): string | null {
  const limits = getUserLimits(user.subscription_tier || 'trial');
  
  if (settings.countries && limits.countries !== -1) {
    if (settings.countries.length > limits.countries) {
      return `A(z) ${user.subscription_tier} csomagban maximum ${limits.countries} ország választható.`;
    }
  }
  
  if (settings.categories && limits.categories !== -1) {
    if (settings.categories.length > limits.categories) {
      return `A(z) ${user.subscription_tier} csomagban maximum ${limits.categories} kategória választható.`;
    }
  }
  
  if (settings.keywords && limits.keywords !== -1) {
    if (settings.keywords.length > limits.keywords) {
      return `A(z) ${user.subscription_tier} csomagban maximum ${limits.keywords} kulcsszó adható meg.`;
    }
  }
  
  return null;
}
```

---

# RÉSZ 6: API ROUTES KIEGÉSZÍTÉS

## 6.1 Registration API (Updated)

```typescript
// app/api/register/route.ts

import { NextRequest, NextResponse } from 'next/server';
import bcrypt from 'bcryptjs';
import { createUser } from '@/lib/db/users';
import { createCheckoutSession } from '@/lib/stripe';

export async function POST(request: NextRequest) {
  const body = await request.json();
  
  const {
    email,
    password,
    language,
    categories,
    countries,
    keywords,
    sources,
    notify_push,
    notify_email,
    notify_telegram,
    onesignal_player_id,
    tier,
    billingCycle,
  } = body;
  
  // Validation
  if (!email || !password) {
    return NextResponse.json({ error: 'Email and password required' }, { status: 400 });
  }
  
  if (!tier || !billingCycle) {
    return NextResponse.json({ error: 'Subscription tier and billing cycle required' }, { status: 400 });
  }
  
  // Hash password
  const password_hash = await bcrypt.hash(password, 12);
  
  // Create user
  try {
    const user = await createUser({
      email,
      password_hash,
      language: language || 'hu',
      categories: categories || [],
      countries: countries || [],
      keywords: keywords || [],
      sources: sources || [],
      notify_push: notify_push ?? true,
      notify_email: notify_email ?? true,
      notify_telegram: notify_telegram ?? false,
      onesignal_player_id,
      subscription_tier: 'trial',
      subscription_status: 'trialing',
      billing_cycle: billingCycle,
      trial_ends_at: new Date(Date.now() + 14 * 24 * 60 * 60 * 1000).toISOString(),
    });
    
    // Create Stripe checkout session
    const checkoutUrl = await createCheckoutSession(
      user.id,
      email,
      tier,
      billingCycle
    );
    
    return NextResponse.json({
      success: true,
      checkoutUrl,
      token: user.token,
      telegram_link: notify_telegram ? `https://t.me/DealSpyBot?start=${user.token}` : null,
    });
    
  } catch (error: any) {
    if (error.message?.includes('duplicate')) {
      return NextResponse.json({ error: 'Email already registered' }, { status: 409 });
    }
    return NextResponse.json({ error: 'Registration failed' }, { status: 500 });
  }
}
```

## 6.2 Billing Portal API

```typescript
// app/api/billing/portal/route.ts

import { NextRequest, NextResponse } from 'next/server';
import { getUserByToken } from '@/lib/db/users';
import { createBillingPortalSession } from '@/lib/stripe';

export async function POST(request: NextRequest) {
  const { token } = await request.json();
  
  const user = await getUserByToken(token);
  
  if (!user) {
    return NextResponse.json({ error: 'User not found' }, { status: 404 });
  }
  
  if (!user.stripe_customer_id) {
    return NextResponse.json({ error: 'No billing information' }, { status: 400 });
  }
  
  const portalUrl = await createBillingPortalSession(user.stripe_customer_id);
  
  return NextResponse.json({ url: portalUrl });
}
```

---

# RÉSZ 7: FRONTEND COMPONENTS

## 7.1 Pricing Card Component

```typescript
// components/PricingCard.tsx

'use client';

import { useState } from 'react';

type Tier = 'starter' | 'pro' | 'enterprise';
type BillingCycle = 'monthly' | 'yearly';

interface PricingCardProps {
  tier: Tier;
  isPopular?: boolean;
  selectedBillingCycle: BillingCycle;
  onSelect: (tier: Tier) => void;
  translations: any;
}

const PRICING = {
  starter: { monthly: 19, yearly: 15 },
  pro: { monthly: 49, yearly: 39 },
  enterprise: { monthly: 149, yearly: 119 },
};

const FEATURES = {
  starter: {
    countries: '1 ország',
    platforms: '3 platform',
    categories: '2 kategória',
    keywords: '5 kulcsszó',
    channels: ['Email digest'],
    support: 'Email support',
  },
  pro: {
    countries: '3 ország',
    platforms: 'Összes platform',
    categories: 'Összes kategória',
    keywords: '20 kulcsszó',
    channels: ['Azonnali push', 'Email', 'Telegram'],
    support: 'Prioritás support',
  },
  enterprise: {
    countries: 'Összes ország',
    platforms: 'Összes platform',
    categories: 'Összes kategória',
    keywords: 'Korlátlan',
    channels: ['Push', 'Email', 'Telegram', 'Slack', 'API'],
    support: 'Dedikált support',
  },
};

export function PricingCard({
  tier,
  isPopular,
  selectedBillingCycle,
  onSelect,
  translations,
}: PricingCardProps) {
  const price = PRICING[tier][selectedBillingCycle];
  const features = FEATURES[tier];
  const yearlyTotal = PRICING[tier].yearly * 12;
  const monthlyTotal = PRICING[tier].monthly * 12;
  const savings = monthlyTotal - yearlyTotal;
  
  return (
    <div className={`
      relative rounded-2xl p-8 
      ${isPopular 
        ? 'bg-gradient-to-b from-blue-600 to-blue-700 text-white ring-4 ring-blue-300 scale-105' 
        : 'bg-white border-2 border-gray-200'
      }
    `}>
      {isPopular && (
        <div className="absolute -top-4 left-1/2 -translate-x-1/2">
          <span className="bg-yellow-400 text-yellow-900 text-sm font-bold px-4 py-1 rounded-full">
            ⭐ NÉPSZERŰ
          </span>
        </div>
      )}
      
      <div className="text-center mb-6">
        <h3 className={`text-2xl font-bold mb-2 ${isPopular ? 'text-white' : 'text-gray-900'}`}>
          {tier.toUpperCase()}
        </h3>
        <div className="flex items-baseline justify-center gap-1">
          <span className={`text-5xl font-bold ${isPopular ? 'text-white' : 'text-gray-900'}`}>
            €{price}
          </span>
          <span className={isPopular ? 'text-blue-200' : 'text-gray-500'}>/hó</span>
        </div>
        {selectedBillingCycle === 'yearly' && (
          <p className={`text-sm mt-2 ${isPopular ? 'text-blue-200' : 'text-green-600'}`}>
            €{savings} megtakarítás évente
          </p>
        )}
      </div>
      
      <ul className="space-y-3 mb-8">
        <li className="flex items-center gap-2">
          <span className={isPopular ? 'text-blue-200' : 'text-green-500'}>✓</span>
          <span>{features.countries}</span>
        </li>
        <li className="flex items-center gap-2">
          <span className={isPopular ? 'text-blue-200' : 'text-green-500'}>✓</span>
          <span>{features.platforms}</span>
        </li>
        <li className="flex items-center gap-2">
          <span className={isPopular ? 'text-blue-200' : 'text-green-500'}>✓</span>
          <span>{features.categories}</span>
        </li>
        <li className="flex items-center gap-2">
          <span className={isPopular ? 'text-blue-200' : 'text-green-500'}>✓</span>
          <span>{features.keywords}</span>
        </li>
        {features.channels.map((channel, i) => (
          <li key={i} className="flex items-center gap-2">
            <span className={isPopular ? 'text-blue-200' : 'text-green-500'}>✓</span>
            <span>{channel}</span>
          </li>
        ))}
        <li className="flex items-center gap-2">
          <span className={isPopular ? 'text-blue-200' : 'text-green-500'}>✓</span>
          <span>{features.support}</span>
        </li>
      </ul>
      
      <button
        onClick={() => onSelect(tier)}
        className={`
          w-full py-3 px-6 rounded-lg font-semibold transition-all
          ${isPopular
            ? 'bg-white text-blue-600 hover:bg-blue-50'
            : 'bg-blue-600 text-white hover:bg-blue-700'
          }
        `}
      >
        {tier === 'enterprise' ? 'Kapcsolatfelvétel' : 'Próbáld ki ingyen'}
      </button>
    </div>
  );
}
```

## 7.2 Billing Cycle Toggle

```typescript
// components/BillingToggle.tsx

'use client';

type BillingCycle = 'monthly' | 'yearly';

interface BillingToggleProps {
  value: BillingCycle;
  onChange: (value: BillingCycle) => void;
}

export function BillingToggle({ value, onChange }: BillingToggleProps) {
  return (
    <div className="flex items-center justify-center gap-4 mb-12">
      <span className={`text-lg ${value === 'monthly' ? 'text-gray-900 font-semibold' : 'text-gray-500'}`}>
        Havi fizetés
      </span>
      
      <button
        onClick={() => onChange(value === 'monthly' ? 'yearly' : 'monthly')}
        className={`
          relative w-16 h-8 rounded-full transition-colors
          ${value === 'yearly' ? 'bg-blue-600' : 'bg-gray-300'}
        `}
      >
        <span className={`
          absolute top-1 w-6 h-6 rounded-full bg-white shadow transition-transform
          ${value === 'yearly' ? 'translate-x-9' : 'translate-x-1'}
        `} />
      </button>
      
      <span className={`text-lg ${value === 'yearly' ? 'text-gray-900 font-semibold' : 'text-gray-500'}`}>
        Éves fizetés
      </span>
      
      {value === 'yearly' && (
        <span className="bg-green-100 text-green-700 text-sm font-medium px-3 py-1 rounded-full">
          20% megtakarítás
        </span>
      )}
    </div>
  );
}
```

---

# RÉSZ 8: TELJES LANDING PAGE COMPONENT

```typescript
// app/page.tsx

'use client';

import { useState } from 'react';
import { PricingCard } from '@/components/PricingCard';
import { BillingToggle } from '@/components/BillingToggle';

export default function LandingPage() {
  const [billingCycle, setBillingCycle] = useState<'monthly' | 'yearly'>('yearly');
  const [language, setLanguage] = useState<'hu' | 'en' | 'de'>('hu');
  
  const handleSelectTier = (tier: 'starter' | 'pro' | 'enterprise') => {
    if (tier === 'enterprise') {
      window.location.href = 'mailto:info@dealspy.eu?subject=Enterprise%20csomag%20érdeklődés';
    } else {
      window.location.href = `/register?tier=${tier}&billing=${billingCycle}`;
    }
  };
  
  return (
    <div className="min-h-screen bg-gradient-to-b from-gray-50 to-white">
      {/* HEADER */}
      <header className="sticky top-0 z-50 bg-white/80 backdrop-blur-md border-b border-gray-200">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex items-center justify-between h-16">
            <div className="flex items-center gap-2">
              <span className="text-2xl">🔍</span>
              <span className="text-xl font-bold text-gray-900">DealSpy.eu</span>
            </div>
            
            <nav className="hidden md:flex items-center gap-8">
              <a href="#features" className="text-gray-600 hover:text-gray-900">Funkciók</a>
              <a href="#pricing" className="text-gray-600 hover:text-gray-900">Árazás</a>
              <a href="#faq" className="text-gray-600 hover:text-gray-900">GYIK</a>
            </nav>
            
            <div className="flex items-center gap-4">
              <div className="flex gap-1 text-sm">
                {(['hu', 'en', 'de'] as const).map((lang) => (
                  <button
                    key={lang}
                    onClick={() => setLanguage(lang)}
                    className={`px-2 py-1 rounded ${language === lang ? 'bg-blue-100 text-blue-700' : 'text-gray-500'}`}
                  >
                    {lang.toUpperCase()}
                  </button>
                ))}
              </div>
              <a href="/login" className="text-gray-600 hover:text-gray-900">Bejelentkezés</a>
              <a href="/register" className="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700">
                Regisztráció
              </a>
            </div>
          </div>
        </div>
      </header>

      {/* HERO */}
      <section className="py-20 md:py-32">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
          <h1 className="text-4xl md:text-6xl font-bold text-gray-900 mb-6">
            Csődvagyon és aukciók<br />
            <span className="text-blue-600">egész Európából.</span>
          </h1>
          <p className="text-xl text-gray-600 mb-8 max-w-3xl mx-auto">
            A DealSpy figyeli helyetted a magyar, osztrák és német csődvagyon portálokat.
            Értesítünk, ha megjelenik amit keresel – legyen az szerver, gép, jármű vagy ingatlan.
          </p>
          <div className="flex flex-col sm:flex-row gap-4 justify-center mb-12">
            <a href="/register" className="bg-blue-600 text-white px-8 py-4 rounded-lg text-lg font-semibold hover:bg-blue-700 transition-colors">
              🚀 Próbáld ki ingyen 14 napig
            </a>
            <a href="#how-it-works" className="border-2 border-gray-300 text-gray-700 px-8 py-4 rounded-lg text-lg font-semibold hover:border-gray-400 transition-colors">
              ▶ Így működik
            </a>
          </div>
          <div className="flex flex-wrap justify-center gap-6 text-sm text-gray-500">
            <span>✓ 10+ platform figyelése</span>
            <span>✓ Napi automatikus frissítés</span>
            <span>✓ Automatikus fordítás</span>
            <span>✓ 30-70% a piaci ár alatt</span>
          </div>
        </div>
      </section>

      {/* PROBLEM/SOLUTION */}
      <section className="py-20 bg-gray-50">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <h2 className="text-3xl font-bold text-center text-gray-900 mb-12">
            Ismered ezt a problémát?
          </h2>
          <div className="grid md:grid-cols-3 gap-8 mb-12">
            {[
              { icon: '😫', title: 'Túl sok platform', desc: '10+ különböző oldalt kellene naponta nézni. Ki ér rá erre?' },
              { icon: '🇩🇪', title: 'Nyelvi akadály', desc: 'A legjobb lehetőségek németül vannak. Ki ér rá ezt fordítgatni?' },
              { icon: '⏰', title: 'Lekésel a dealekről', desc: 'Mire észreveszed, már lejárt az aukció vagy más vitte el.' },
            ].map((item, i) => (
              <div key={i} className="bg-white p-8 rounded-2xl shadow-sm text-center">
                <div className="text-5xl mb-4">{item.icon}</div>
                <h3 className="text-xl font-semibold text-gray-900 mb-2">{item.title}</h3>
                <p className="text-gray-600">{item.desc}</p>
              </div>
            ))}
          </div>
          <div className="bg-blue-600 text-white p-8 rounded-2xl text-center max-w-2xl mx-auto">
            <div className="text-4xl mb-4">💡</div>
            <h3 className="text-2xl font-bold mb-4">A DealSpy megoldja mindezt.</h3>
            <p className="text-blue-100">
              Egyetlen felületen figyeljük az összes releváns platformot,
              automatikusan lefordítjuk a találatokat, és azonnal értesítünk.
            </p>
          </div>
        </div>
      </section>

      {/* HOW IT WORKS */}
      <section id="how-it-works" className="py-20">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <h2 className="text-3xl font-bold text-center text-gray-900 mb-12">
            Hogyan működik?
          </h2>
          <div className="grid md:grid-cols-3 gap-8">
            {[
              { step: '①', icon: '⚙️', title: 'BEÁLLÍTASZ', desc: 'Válaszd ki a kategóriákat, országokat és kulcsszavakat.', time: '⏱️ 2 perc beállítás' },
              { step: '②', icon: '🔍', title: 'FIGYEL', desc: 'A DealSpy naponta végigfésüli a platformokat.', time: '🤖 Teljesen automatikus' },
              { step: '③', icon: '🔔', title: 'ÉRTESÍT', desc: 'Azonnal értesítünk push, email vagy Telegram üzenetben.', time: '📱 Valós idejű alertek' },
            ].map((item, i) => (
              <div key={i} className="text-center">
                <div className="text-4xl text-gray-400 mb-4">{item.step}</div>
                <div className="w-20 h-20 bg-blue-100 rounded-full flex items-center justify-center text-4xl mx-auto mb-4">
                  {item.icon}
                </div>
                <h3 className="text-xl font-semibold text-gray-900 mb-2">{item.title}</h3>
                <p className="text-gray-600 mb-4">{item.desc}</p>
                <span className="text-sm text-blue-600">{item.time}</span>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* SOURCES */}
      <section className="py-20 bg-gray-50">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <h2 className="text-3xl font-bold text-center text-gray-900 mb-4">
            Ezeket a platformokat figyeljük
          </h2>
          <p className="text-gray-600 text-center mb-12">Folyamatosan bővülő forráslista</p>
          
          <div className="space-y-8">
            {[
              { flag: '🇭🇺', country: 'Magyarország', platforms: [{ name: 'EÉR', desc: 'Hivatalos csődvagyon értékesítési platform' }] },
              { flag: '🇦🇹', country: 'Ausztria', platforms: [{ name: 'Ediktsdatei', desc: 'Bírósági hirdetmények' }, { name: 'KSV1870', desc: 'Hitelezővédelmi csődlista' }] },
              { flag: '🇩🇪', country: 'Németország', platforms: [{ name: 'Insolvenzbekanntmachungen', desc: 'Hivatalos csődhirdetések' }, { name: 'NetBid', desc: 'Ipari aukciók' }, { name: 'Proventura', desc: 'Gépek, IT, járművek' }] },
              { flag: '🇪🇺', country: 'EU piacterek', platforms: [{ name: 'Machineseeker', desc: 'Európa #1 géppiac' }, { name: 'Mascus', desc: 'Építőipari gépek' }, { name: 'TruckScout24', desc: 'Haszongépjárművek' }] },
            ].map((group, i) => (
              <div key={i}>
                <h3 className="text-lg font-semibold text-gray-900 mb-4">{group.flag} {group.country}</h3>
                <div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-4">
                  {group.platforms.map((platform, j) => (
                    <div key={j} className="bg-white p-4 rounded-lg border border-gray-200">
                      <div className="font-medium text-gray-900">{platform.name}</div>
                      <div className="text-sm text-gray-500">{platform.desc}</div>
                    </div>
                  ))}
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* PRICING */}
      <section id="pricing" className="py-20">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <h2 className="text-3xl font-bold text-center text-gray-900 mb-4">
            Egyszerű, átlátható árazás
          </h2>
          <p className="text-gray-600 text-center mb-8">
            14 napos ingyenes próbaidőszak minden csomaghoz. Bankkártya nélkül.
          </p>
          
          <BillingToggle value={billingCycle} onChange={setBillingCycle} />
          
          <div className="grid md:grid-cols-3 gap-8 items-start">
            <PricingCard tier="starter" selectedBillingCycle={billingCycle} onSelect={handleSelectTier} translations={{}} />
            <PricingCard tier="pro" isPopular selectedBillingCycle={billingCycle} onSelect={handleSelectTier} translations={{}} />
            <PricingCard tier="enterprise" selectedBillingCycle={billingCycle} onSelect={handleSelectTier} translations={{}} />
          </div>
          
          <div className="text-center mt-8 text-gray-500">
            💳 Biztonságos fizetés Stripe-on keresztül | 🔄 Bármikor lemondható | 💰 14 napos pénzvisszafizetés
          </div>
        </div>
      </section>

      {/* FAQ */}
      <section id="faq" className="py-20 bg-gray-50">
        <div className="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
          <h2 className="text-3xl font-bold text-center text-gray-900 mb-12">
            Gyakran ismételt kérdések
          </h2>
          <div className="space-y-4">
            {[
              { q: 'Hogyan működik a 14 napos próbaidőszak?', a: 'A regisztráció után 14 napig ingyenesen használhatod a választott csomag összes funkcióját. Bankkártya adatokat csak a próbaidőszak végén kérünk.' },
              { q: 'Milyen fizetési módokat fogadtok el?', a: 'Bankkártyás fizetést (Visa, Mastercard, American Express) a Stripe biztonságos platformján keresztül.' },
              { q: 'Lemondhatom bármikor az előfizetést?', a: 'Igen, az előfizetés bármikor lemondható. A lemondás után a már kifizetett időszak végéig használhatod a szolgáltatást.' },
              { q: 'Milyen gyakran frissülnek az adatok?', a: 'Naponta egyszer (scrape 09:30 CET, értesítés 10:00 CET, digest 15:00 CET).' },
              { q: 'Van API hozzáférés?', a: 'Az Enterprise csomagban igen. REST API-n keresztül saját rendszerbe integrálhatod a deal feed-et.' },
            ].map((item, i) => (
              <details key={i} className="bg-white rounded-lg border border-gray-200">
                <summary className="p-4 font-medium text-gray-900 cursor-pointer hover:bg-gray-50">
                  {item.q}
                </summary>
                <p className="px-4 pb-4 text-gray-600">{item.a}</p>
              </details>
            ))}
          </div>
        </div>
      </section>

      {/* FINAL CTA */}
      <section className="py-20 bg-blue-600">
        <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
          <h2 className="text-3xl font-bold text-white mb-4">
            Ne maradj le a következő jó üzletről
          </h2>
          <p className="text-blue-100 mb-8">
            Csatlakozz azokhoz, akik már automatikusan kapják a legjobb csődvagyon és aukciós lehetőségeket.
          </p>
          <a href="/register" className="inline-block bg-white text-blue-600 px-8 py-4 rounded-lg text-lg font-semibold hover:bg-blue-50 transition-colors">
            🚀 Ingyenes próba indítása – bankkártya nélkül
          </a>
          <div className="mt-8 flex flex-wrap justify-center gap-6 text-sm text-blue-200">
            <span>🔒 256-bit SSL titkosítás</span>
            <span>🇪🇺 GDPR kompatibilis</span>
            <span>⭐ 4.8/5 értékelés</span>
          </div>
        </div>
      </section>

      {/* FOOTER */}
      <footer className="py-12 bg-gray-900 text-gray-400">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="grid md:grid-cols-4 gap-8 mb-8">
            <div>
              <div className="flex items-center gap-2 mb-4">
                <span className="text-2xl">🔍</span>
                <span className="text-xl font-bold text-white">DealSpy.eu</span>
              </div>
              <p className="text-sm">EU csődvagyon monitoring</p>
            </div>
            <div>
              <h4 className="text-white font-semibold mb-4">Termék</h4>
              <ul className="space-y-2 text-sm">
                <li><a href="#features" className="hover:text-white">Funkciók</a></li>
                <li><a href="#pricing" className="hover:text-white">Árazás</a></li>
                <li><a href="#faq" className="hover:text-white">GYIK</a></li>
              </ul>
            </div>
            <div>
              <h4 className="text-white font-semibold mb-4">Céginfo</h4>
              <ul className="space-y-2 text-sm">
                <li><a href="/impressum" className="hover:text-white">Impressum</a></li>
                <li><a href="/privacy" className="hover:text-white">Adatvédelem</a></li>
                <li><a href="/terms" className="hover:text-white">ÁSZF</a></li>
              </ul>
            </div>
            <div>
              <h4 className="text-white font-semibold mb-4">Kapcsolat</h4>
              <ul className="space-y-2 text-sm">
                <li>info@dealspy.eu</li>
              </ul>
            </div>
          </div>
          <div className="border-t border-gray-800 pt-8 text-center text-sm">
            © 2026 DealSpy.eu | Progile Tanácsadó Kft. | Minden jog fenntartva
          </div>
        </div>
      </footer>
    </div>
  );
}
```

---

# RÉSZ 9: ÖSSZEFOGLALÓ CHECKLIST

## Stripe Setup
- [ ] Stripe account létrehozása
- [ ] Products és Prices létrehozása (6 db)
- [ ] Webhook beállítása
- [ ] Customer Portal beállítása
- [ ] Price ID-k .env-be

## Database
- [ ] Users tábla bővítése (subscription mezők)
- [ ] Payments tábla létrehozása

## API Routes
- [ ] /api/register (updated with Stripe)
- [ ] /api/stripe/webhook
- [ ] /api/billing/portal

## Frontend
- [ ] Landing page komponensek
- [ ] PricingCard komponens
- [ ] BillingToggle komponens
- [ ] Registration wizard (4 lépés)
- [ ] Success page

## Legal
- [ ] ÁSZF oldal
- [ ] Privacy Policy oldal
- [ ] Cookie Policy
- [ ] Impressum

---

**Ez a teljes landing page és payment specifikáció. A Cursor-ban ezt külön fájlként töltsd be a fejlesztéshez.**
